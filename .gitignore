# OAuth tokens and sensitive credentials
google_token.json
ms_token_cache.bin
google_credentials.json

# User configuration (contains client_id/tenant_id)
config.json

# Sync state
sync_state.db

# Logs
*.log

# Python
__pycache__/
*.pyc
*.pyo

# Test and lint tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/

# Build and distribution artifacts
*.egg-info/
dist/
build/
*.spec

# Compiled executables (generated by build.bat via PyInstaller)
# Distribute via GitHub Releases, not in the repository
GCalSync.exe
monitor.exe
setup.exe
sync.exe

# Installer output (generated by installer.iss via Inno Setup ISCC)
# Distribute GCalSync-Setup.exe via GitHub Releases, not in the repository
installer/
# Environment variables
.env

# Claude Code (local IDE settings)
.claude/
